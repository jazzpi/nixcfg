# nixpkgs knowledge

Findings about nixpkgs module/package/lib internals, written by the `nixpkgs-researcher`
subagent. One file per module, package, or lib topic, kebab-case
(e.g. `services-foo.md`, `lib-attrsets.md`). See `../README.md` for the workflow and
note format.
