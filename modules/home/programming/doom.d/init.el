(doom! :completion
       (vertico +icons)
       :emacs
       undo
       :ui
       doom
       modeline
       :editor
       (evil +everywhere)
       :tools
       magit
       :config
       (default +bindings))
